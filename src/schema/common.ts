export type MutationResponseDto = {
    id: string,
    executionArn?: string
}

export type ImageDto = {
    id: string,
    name: string
}

export type AudioDto = {
    key: string,
    markup: string,
    voice: string
}